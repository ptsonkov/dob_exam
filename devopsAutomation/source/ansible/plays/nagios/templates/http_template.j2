define service {
        use                             remote-service         
        host_name                       {{ rhost }}
        service_description             HTTP
        check_command                   check_http!$HOSTADDRESS$
        notifications_enabled           0
}